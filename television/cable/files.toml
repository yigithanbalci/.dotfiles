[metadata]
name = "files"
description = "Open selected file in $EDITOR (vim)"

[source]
command = "fd -t f"

[preview]
command = "bat -n --color=always '{}'"

[keybindings]
enter = "actions:edit"

[actions.edit]
description = "Open in editor"
command = "${EDITOR:-vim} '{}'"
mode = "execute"
