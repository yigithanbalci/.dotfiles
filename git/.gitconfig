[include] 
  path = ~/.gitconfig.local

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[gpg]
	format = ssh
[commit]
	gpgsign = true
[tag]
	gpgsign = true
[user]
	signingkey = ~/.ssh/github_noreply_yigithanbalci.pub
	name = yigithanbalciacc
	email = 172791293+yigithanbalciacc@users.noreply.github.com
[credential "https://github.com"]
  helper = !gh auth git-credential
[credential "https://gist.github.com"]
  helper = !gh auth git-credential
[merge]
	tool = vimdiff
[pull]
	rebase = true
[core]
	autocrlf = input
[pull]
	rebase = false
[alias]
  st = status
  co = checkout
  br = branch
  adp = add -p
  ci = commit
  cm = commit -S -m
  cmp = commit -mp
  lg = log --oneline --graph --all --decorate
  rbs = "!f() { git rebase -i --keep-empty --no-autostash --rebase-merges \"$@\"; }; f"
  prn = !sh -c 'git fetch -p && git remote prune origin && git branch -vv | grep \": gone]\" | awk \"{print \\$1}\" | xargs -r git branch -D'
